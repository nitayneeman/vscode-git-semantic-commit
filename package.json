{
  "name": "git-semantic-commit-message",
  "displayName": "Git - Semantic Commit Message",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.38.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:gitSemanticCommitMessage.semanticCommit"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "type": "object",
      "title": "Git - Semantic Commit Message",
      "properties": {
        "gitSemanticCommitMessage.commitOptions": {
          "type": "string",
          "default": "--quiet",
          "markdownDescription": "Specifies which [arguments](https://git-scm.com/docs/git-commit#_options) to be passed when the commit is executed."
        },
        "gitSemanticCommitMessage.preserveScope": {
          "type": "boolean",
          "default": "false",
          "markdownDescription": "Determines whether to preserve the last message scope that was inserted."
        },
        "gitSemanticCommitMessage.stageAll": {
          "type": "boolean",
          "default": "true",
          "markdownDescription": "Determines whether to stage all changes before the commit, in case the staging area (index) is empty."
        },
        "gitSemanticCommitMessage.types": {
          "type": "array",
          "default": [
            "chore",
            "docs",
            "feat",
            "fix",
            "refactor",
            "style",
            "test"
          ],
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "uniqueItems": true,
          "markdownDescription": "Specifies the supported message types."
        }
      }
    },
    "commands": [
      {
        "command": "gitSemanticCommitMessage.semanticCommit",
        "title": "Git: Semantic Commit",
        "icon": {
          "dark": "images/navigation/dark-icon.svg",
          "light": "images/navigation/light-icon.svg"
        }
      }
    ],
    "menus": {
      "scm/title": [
        {
          "when": "scmProvider == git",
          "command": "gitSemanticCommitMessage.semanticCommit",
          "alt": "gitSemanticCommitMessage.semanticCommit",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/execa": "^2.0.0",
    "@types/glob": "^7.1.1",
    "@types/mocha": "^5.2.6",
    "@types/node": "^10.12.21",
    "@types/vscode": "^1.38.0",
    "glob": "^7.1.4",
    "mocha": "^6.1.4",
    "tslint": "^5.12.1",
    "typescript": "^3.3.1",
    "vscode-test": "^1.2.0"
  },
  "dependencies": {
    "execa": "^2.0.5"
  }
}
